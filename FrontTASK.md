# 📄 PROJ: Profit Power — Frontend MVP Specification
**Ticket:** [PP-FE-010] Initialize & Build Core Frontend Features
**Assignee:** @FrontendDeveloper
**Status:** 🚀 Ready for Dev
**Priority:** High

---

## 1. Обзор (Overview)
Мы разрабатываем клиентскую часть приложения "Profit Power". Бэкенд полностью готов и протестирован.
**Цель спринта:** Реализовать авторизацию, защищенный доступ к дашборду и базовый функционал учета транзакций.

---

## 2. Технический стек и Архитектура (Architecture Guidelines)
Мы строго следуем утвержденным правилам:
*   **Framework:** Next.js 14 (App Router).
*   **Styling:** Tailwind CSS (utility) + SCSS Modules (complex components).
*   **Structure:** Modified FSD (`app`, `widgets`, `features`, `shared`).
*   **Data Fetching:**
    *   Серверные компоненты (RSC) для получения данных (`GET`).
    *   Server Actions для мутаций (`POST`, `DELETE`).
*   **Forms:** `useActionState` (React 19 hook) + Zod validation.

---

## 3. Документация API (Backend Interface)
Бэкенд доступен по адресу: `http://localhost:5000/api`
**Важно:** Для всех защищенных запросов необходимо передавать заголовок `Authorization: Bearer <accessToken>`. Токен мы храним в памяти/куках (об этом ниже).

### 🔐 Auth
| Method | Endpoint | Description | Payload (Body) |
| :--- | :--- | :--- | :--- |
| `POST` | `/auth/register` | Регистрация нового юзера | `{ email, password, name }` |
| `POST` | `/auth/login` | Вход в систему | `{ email, password }` |
| `POST` | `/auth/logout` | Выход (чистка cookie) | - |
| `GET` | `/auth/refresh` | Обновление токена | (Cookie `refreshToken` отправляется браузером) |

### 📂 Categories
| Method | Endpoint | Description | Payload |
| :--- | :--- | :--- | :--- |
| `GET` | `/categories` | Список всех категорий юзера | - |
| `POST` | `/categories` | Создать категорию | `{ name, icon }` |
| `DELETE`| `/categories/:id`| Удалить категорию | - |

### 💸 Transactions
| Method | Endpoint | Query Params | Description |
| :--- | :--- | :--- | :--- |
| `GET` | `/transactions` | `page`, `limit`, `startDate`, `endDate`, `search` | Получение списка с пагинацией и фильтрами |
| `POST` | `/transactions` | - | `{ amount, type, category, date, description }` |
| `DELETE`| `/transactions/:id`| - | Удаление транзакции |

---

## 4. Задачи к выполнению (Task Breakdown)

### Task 1: API Infrastructure (Layer: Shared)
Нужно настроить "мост" между Next.js и Express API.
*   **Требования:**
    *   Создать инстанс `fetch` (или функцию-обертку), которая автоматически подставляет `Base URL`.
    *   Реализовать перехватчик (interceptor) на клиенте/сервере: если API возвращает `401 Unauthorized`, мы должны пробовать сделать запрос на `/auth/refresh`, получить новый токен и повторить исходный запрос.
    *   Настроить типизацию ответов (использовать DTO из `shared/types`).

### Task 2: Authentication Features (Layer: Features/Pages)
Реализовать формы входа и регистрации.
*   **UI:** Использовать готовые `Input` и `Button` из `shared/ui`. Дизайн минималистичный, по центру экрана.
*   **Logic:**
    *   Валидация полей через Zod (email — email, password — min 6 chars).
    *   При успешном входе — редирект на `/home`.
    *   При входе на `/` или `/login` авторизованного юзера — редирект на `/home`.

### Task 3: App Shell & Navigation (Layer: Widgets/Layouts)
Сверстать основной макет приватной зоны (`(private)/layout.tsx`).
*   **Sidebar (Desktop):** Логотип, навигация (Home, History, Analytics, Settings), профиль юзера внизу.
*   **Bottom Nav (Mobile):** Фиксированная панель снизу. Кнопка "+" (Добавить транзакцию) должна быть **выделена** и находиться по центру.
*   **Header:** Заголовок текущей страницы.

### Task 4: Transaction Management (Layer: Features/Entities)
Реализация ядра приложения.
*   **Create Transaction Modal:**
    *   Модальное окно поверх любой страницы.
    *   Поля: Тип (Доход/Расход), Сумма, Категория (выпадающий список, данные берем с API), Дата, Описание.
*   **Transaction List (Dashboard):**
    *   Отображение списка транзакций.
    *   Группировка по датам (Сегодня, Вчера, и т.д. — *Logic on Frontend*).
    *   Красивые иконки категорий.

---

## 5. Definition of Done (DoD)
1.  Код написан на TypeScript без `any`.
2.  Приложение запускается через `npm run dev`.
3.  Нет ошибок в консоли браузера.
4.  Стили адаптивны (Mobile First).